\chapter{Desarrollo}

\section{Preparación de los datos}

\subsection{Obtención de los datos}

Esta sección describe cómo se han obtenido los datos del proyecto. Primero se presenta la fuente de datos utilizada y su estructura, y después se explica el proceso de descarga y construcción del dataset.

\subsubsection{Fuente de datos}

La fuente de datos principal utilizada para la realización de este trabajo es StatsBomb Open Data, un repositorio público disponible en GitHub que proporciona conjuntos de datos e investigaciones para mejorar la comprensión del fútbol.

Los datos de este repositorio están estructurados en tres niveles jerárquicos.
\begin{itemize}
    \item \textbf{Competiciones} (\texttt{data/competitions.json}), que contiene el catálogo completo de competiciones y temporadas disponibles. Entre los campos que incluye cada entrada destacan el identificador de la competición (\texttt{competition\_id}), el identificador de la temporada (\texttt{season\_id}), el nombre de la competición, el nombre de la temporada y el género de la competición, entre otros.

    \item \textbf{Partidos} (\texttt{data/matches/\{competition\_id\}/\{season\_id\}.json}), que contiene los metadatos de todos los partidos de una competición y temporada concretas. Entre los campos disponibles en cada entrada destacan el identificador del partido (\texttt{match\_id}), los equipos local y visitante, el marcador final, la fecha, la jornada, el estadio, el árbitro y el estado del partido, entre otros. Solo los partidos con estado \textit{available} tienen fichero de eventos asociado y han sido incluidos en el dataset.

    \item \textbf{Eventos} (\texttt{data/events/\{match\_id\}.json}), que contiene el registro completo de acciones de un partido, ordenadas cronológicamente. Cada acción incluye el tipo de evento, el equipo, el minuto y segundo de juego, la localización en el campo y los atributos específicos del tipo de acción. Es el fichero principal del que se extrae la información para construir el dataset de modelado.
\end{itemize}

El repositorio también incluye otros ficheros de alineaciones y datos de posicionamiento 360° que no se utilizan en este trabajo ya que no son necesarios para el enfoque planteado. La especificación técnica completa de los campos de cada tipo de fichero está recogida en la carpeta \texttt{doc/} del repositorio, con documentos específicos para competiciones, partidos y eventos.

\subsubsection{Proceso de descarga y construcción del dataset}

El dataset utilizado para este proyecto se ha construido utilizando un script de Python que automatiza la descarga y el almacenamiento de los datos del repositorio StatsBomb Open Data.

El primer paso para generar este dataset consiste en descargar el catálogo de competiciones y para competición se descarga el fichero de partidos disponibles. Por cada partido, se obtiene el fichero de eventos correspondiente y se itera sobre cada acción registrada. Para cada evento se extrae un conjunto de campos comunes y unos campos específicos según el tipo de acción. Cada acción pertenece a una fila del dataset. Al ejecutar este script, se obtiene un fichero csv con todos los eventos de cada partido disponible. La columna \texttt{final\_result}, derivada del marcador final de cada partido, actúa como etiqueta de clasificación y toma los valores \texttt{home\_win}, \texttt{draw} o \texttt{away\_win} según si el equipo local ganó, empató o perdió.

Dado que el proceso implica miles de peticiones a un servidor externo, se han tomado algunas medidas para hacerlo más resistente a fallos de red. Ante un error de descarga, el script reintenta la petición hasta tres veces con pausas progresivas de 5, 10 y 15 segundos antes de registrar el fallo. Para evitar perder el trabajo acumulado en caso de un error tardío, el dataset se guarda de forma parcial al terminar cada competición. Por último, se ha implementado una función de verificación que cruza los partidos del dataset con los disponibles en el repositorio y genera automáticamente el comando necesario para recuperar cualquier partido que falte.

\subsection{Comprensión y análisis de los datos}

\subsubsection{Estructura del dataset}

El dataset resultante es un fichero tabular con 12.188.949 filas y 28 columnas, donde cada fila representa un evento individual registrado durante un partido. El conjunto abarca 3.464 partidos de 21 competiciones distintas, que incluyen ligas masculinas de élite, competiciones femeninas y torneos internacionales.

La densidad de eventos es consistente entre partidos, con una media de 3.519 eventos por partido, un mínimo de 2.101 y un máximo de 5.190. Esta regularidad indica que no existen partidos con registros incompletos o truncados que pudieran introducir ruido en las métricas acumuladas.

\subsubsection{Tipos de evento}

El dataset contiene 33 tipos de evento con una distribución muy desigual. Los tres más frecuentes (\textit{Pass}, \textit{Ball Receipt} y \textit{Carry}) acumulan el 75\% del total y corresponden a acciones de seguimiento de posesión. En el extremo opuesto, los tiros (\textit{Shot}) suponen solo el 0,7\%, pero concentran la información más predictiva, ya que son los únicos eventos con \textit{expected goals} (xG) asociado. La tabla~\ref{tab:event_types} recoge la distribución de los diez tipos más frecuentes.

\begin{table}[H]
    \centering
    \begin{tabular}{lrr}
        \hline
        \textbf{Tipo de evento} & \textbf{Eventos} & \textbf{\% del total} \\
        \hline
        Pass                    & 3.387.760        & 27,8\%                \\
        Ball Receipt*           & 3.167.310        & 26,0\%                \\
        Carry                   & 2.632.570        & 21,6\%                \\
        Pressure                & 1.113.859        & 9,1\%                 \\
        Ball Recovery           & 366.673          & 3,0\%                 \\
        Duel                    & 257.861          & 2,1\%                 \\
        Clearance               & 158.993          & 1,3\%                 \\
        Block                   & 132.352          & 1,1\%                 \\
        Dribble                 & 122.047          & 1,0\%                 \\
        Shot                    & 88.023           & 0,7\%                 \\
        \hline
        \multicolumn{3}{l}{\small Fuente: elaboración propia a partir de StatsBomb Open Data.}
    \end{tabular}
    \caption{Frecuencia de los principales tipos de evento en el dataset}
    \label{tab:event_types}
\end{table}

\subsubsection{Balance de clases}

La variable objetivo del problema es el resultado final del partido, que puede tomar tres valores, victoria local (\texttt{home\_win}), empate (\texttt{draw}) y victoria visitante (\texttt{away\_win}). Su distribución sobre los 3.464 partidos del dataset revela un desequilibrio moderado entre clases.

La clase mayoritaria es la victoria local, con 1.565 partidos (45,2\%), seguida de la victoria visitante con 1.102 (31,8\%) y el empate con 797 (23,0\%). El ratio entre la clase más frecuente y la menos frecuente es de 1,96, lo que indica que la clase mayoritaria casi dobla a la minoritaria.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{assets/figuras/eda/balance_clases.pdf}
    \caption{Distribución de la variable objetivo en el dataset}
    \label{fig:balance_clases}
\end{figure}

El desequilibrio tiene dos implicaciones directas para el modelado. Un clasificador que predijera siempre \texttt{home\_win} obtendría una precisión del 42,9\% sin aprender ningún patrón, por lo que el \textit{accuracy} por sí solo no es una métrica válida y se usarán F1-score macro y ponderado en su lugar. Además, se aplicará \texttt{class\_weight='balanced'} en los clasificadores que lo soporten para penalizar más los errores en las clases minoritarias.

El análisis por separado de las competiciones femeninas y masculinas muestra diferencias relevantes. En las masculinas (2.924 partidos) la distribución es 44,8\% victorias locales, 31,0\% visitantes y 24,2\% empates. En las femeninas (540 partidos) los empates caen al 16,7\% y las victorias visitantes suben al 36,1\%. Esta diferencia justifica incluir una variable binaria que identifique el tipo de competición en el dataset de modelado.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.80\textwidth]{assets/figuras/eda/balance_genero.pdf}
    \caption{Distribución de resultados por género de competición}
    \label{fig:balance_genero}
\end{figure}

\subsubsection{Análisis temporal}

El modelo predice el resultado a partir del estado acumulado del partido hasta un minuto determinado. Antes de construir ese dataset de modelado, conviene verificar que los datos registran la evolución del partido de forma homogénea y decidir cómo tratar los eventos que quedan fuera de los 90 minutos reglamentarios.

Al observar la distribución del minuto de cada evento se aprecia un comportamiento que encaja con lo que ocurre en un partido real. Los picos de actividad se producen al inicio de cada parte, en los minutos 1 y 46, cuando se realizan los saques de centro, y a partir de ahí la densidad de eventos se mantiene estable hasta el final del tiempo reglamentario. No hay ningún periodo del partido con un volumen de eventos anómalamente bajo o alto.

Lo que sí requiere una decisión explícita es qué hacer con los eventos registrados pasado el minuto 90. El dataset incluye partidos con prórroga, ya que el minuto máximo registrado es el 139, y prácticamente todos los partidos tienen al menos algún evento fuera de los 90 minutos reglamentarios. Dado que el tiempo de descuento varía según las interrupciones de cada partido y la prórroga no tiene duración fija, mantener esos eventos complicaría la construcción del dataset sin aportar una mejora clara. Por ello se opta por truncar al minuto 90, lo que afecta al 3,2\% de los eventos y deja una base temporal homogénea para todos los partidos.

Respecto a los goles, la segunda parte acumula algo más de la mitad del total, 4.978 de 9.150, frente a los 4.172 de la primera. El minuto 47, el primero tras el descanso, es el de mayor densidad de goles de todo el dataset. Este comportamiento confirma que el momento del partido en el que se está haciendo la predicción aporta información relevante al modelo. La tabla~\ref{tab:temporal} recoge los principales indicadores del análisis.

\begin{table}[H]
    \centering
    \begin{tabular}{lr}
        \hline
        \textbf{Indicador}                                     & \textbf{Valor} \\
        \hline
        Eventos en tiempo reglamentario ($\leq 90$ min)        & 96,8\%         \\
        Eventos en tiempo de descuento / prórroga ($> 90$ min) & 3,2\%          \\
        Partidos con algún evento en minuto $> 90$             & 3.354 de 3.464 \\
        Minuto máximo registrado                               & 139            \\
        \hline
        Goles en primera parte (min.\ 1--45)                   & 4.172 (45,6\%) \\
        Goles en segunda parte (min.\ 46--90)                  & 4.978 (54,4\%) \\
        Minuto con más goles                                   & 47 (143 goles) \\
        \hline
    \end{tabular}
    \caption{Principales indicadores del análisis temporal}
    \label{tab:temporal}
\end{table}

\subsubsection{xG y features discriminativas}

Para decidir qué variables incluir en el modelo, se ha analizado cuáles de las métricas del partido guardan mayor relación con el resultado. El método utilizado consiste en calcular, para cada partido, la diferencia entre el valor acumulado del equipo local y el del visitante en cada métrica, y medir cuánto se correlaciona esa diferencia con haber ganado o perdido.

El xG es la métrica que mejor separa los resultados, con una correlación de $r = 0{,}568$ entre la diferencia de xG y el resultado del partido. Le siguen los tiros ($r = 0{,}357$) y los pases ($r = 0{,}289$). En los tres casos el patrón es claro, ya que el equipo que acaba ganando acumula más xG, más tiros y más pases que el que pierde. La figura~\ref{fig:correlacion_features} muestra el valor de correlación para cada métrica.

Las presiones y las faltas van en sentido contrario. La diferencia entre el local y el visitante correlaciona negativamente con el resultado ($r = -0{,}223$ y $r = -0{,}199$), lo que significa que el equipo local presiona más y comete más faltas en los partidos que pierde. La explicación es táctica, ya que el equipo que va perdiendo aumenta su intensidad defensiva para intentar recuperar el balón, lo que se traduce en más presiones y más faltas. Estas métricas son útiles para el modelo, pero hay que tener en cuenta que el equipo que más presiona no es el que va ganando, sino el que va perdiendo.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{assets/figuras/eda/correlacion_features.pdf}
    \caption{Correlación de Pearson entre la diferencia de cada métrica acumulada y el resultado del partido}
    \label{fig:correlacion_features}
\end{figure}

La tasa de pases completados también varía según el resultado. El equipo ganador completa alrededor de cuatro puntos porcentuales más de sus pases que el perdedor, tanto cuando gana el local (78,6\% frente a 74,0\%) como cuando gana el visitante (77,4\% frente a 74,3\%). Completar más pases es un indicador de control del partido, y el dato confirma que esta métrica aporta información útil al modelo.

Finalmente, conviene remarcar que el 35,3\% de los partidos se decide por un solo gol, lo que hace que muchos encuentros sean difíciles de predecir hasta el final. Además, 231 partidos terminan 0-0, y en esos casos el modelo no dispone de diferencial de goles ni de xG relevante, por lo que tendrá que apoyarse en métricas de posesión y control como los pases, las presiones o los duelos.

\subsubsection{Marcador en curso}

Entre todas las variables que se pueden construir a partir del dataset, el marcador en el minuto $t$ es la más predictiva para un modelo de probabilidad de victoria. Si un equipo gana por dos goles en el minuto 80, la probabilidad de que mantenga el resultado es muy alta independientemente de cualquier otra métrica. Sin embargo, el marcador en curso no existe como campo en el dataset, ya que los datos de StatsBomb registran eventos individuales, no el estado acumulado del partido.

El marcador puede derivarse acumulando los eventos de tipo \textit{Shot} cuyo \texttt{shot\_outcome} sea \texttt{Goal}, ordenados por minuto. Para cada minuto del partido se cuenta cuántos goles ha marcado cada equipo hasta ese momento, lo que produce una curva escalonada que refleja la evolución del marcador a lo largo del encuentro. La figura~\ref{fig:marcador_en_curso} muestra esta evolución para un partido de ejemplo, junto con la diferencia de goles en cada minuto.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{assets/figuras/eda/marcador_en_curso.pdf}
    \caption{Evolución del marcador en curso y diferencia de goles para un partido de ejemplo}
    \label{fig:marcador_en_curso}
\end{figure}

Este análisis confirma que la derivación es viable para todos los partidos del dataset y que los goles están correctamente registrados como eventos \textit{Shot} con resultado \texttt{Goal}. El marcador en curso será una de las variables construidas en la fase de preparación de datos.

\subsubsection{Calidad de los datos}

El análisis de valores ausentes refleja una estructura dispersa habitual en datasets de eventos deportivos, donde cada columna solo tiene valores para el subconjunto de filas correspondiente a su tipo de evento. La columna \texttt{xg} presenta un 99,28\% de nulos porque únicamente se registra en tiros; \texttt{pass\_length} y \texttt{pass\_angle} tienen un 72,21\% de nulos al aplicar solo a pases; y \texttt{card\_type} alcanza el 99,88\% al estar reservada para tarjetas. Todos estos nulos son estructuralmente esperables y no representan un problema de calidad.

Los campos clave para el modelado no presentan ningún valor ausente, lo que confirma la integridad del dataset. Entre ellos se encuentran \texttt{match\_id}, \texttt{minute}, \texttt{event\_type}, \texttt{is\_home} y \texttt{final\_result}. Las columnas de localización (\texttt{loc\_x}, \texttt{loc\_y}) muestran un 0,75\% de nulos, correspondientes a eventos administrativos como el inicio de periodo, que no tienen posición en el campo asociada.

\subsubsection{Limitaciones del dataset}

El análisis exploratorio ha permitido identificar las principales limitaciones del dataset, algunas de las cuales ya se han comentado en apartados anteriores junto con las decisiones adoptadas para mitigarlas.

La limitación más relevante que no se ha abordado todavía es el sesgo hacia el fútbol de élite. Todos los partidos del dataset pertenecen a competiciones profesionales de alto nivel, principalmente ligas europeas y torneos internacionales. La Liga española es la más representada, con 868 partidos (el 25,1\% del total), lo que introduce cierta concentración geográfica. Los patrones aprendidos por el modelo pueden no generalizarse bien a ligas de niveles inferiores o con estilos de juego muy distintos. Para el objetivo de este trabajo esto no supone un problema, pero es un factor a tener en cuenta si se quisiera aplicar el modelo fuera del contexto de las competiciones incluidas.

Las demás limitaciones ya han sido tratadas a lo largo del análisis. La diferencia entre el fútbol femenino y masculino se aborda incluyendo una variable binaria en el dataset de modelado (apartado de balance de clases). La ausencia del marcador en tiempo real se resuelve derivándolo a partir de los eventos de tipo \textit{Shot} con resultado \texttt{Goal} (apartado de marcador en curso). El tratamiento temporal aplicado al minuto 90 afecta al 3,2\% de los eventos y deja fuera los eventos correspondientes a prórroga o penaltis como variables de entrada, aunque los partidos asociados pueden mantenerse en el dataset. Esto puede introducir cierto ruido en la variable objetivo para ese subconjunto (apartado de análisis temporal). Por último, la estructura dispersa del dataset requiere una transformación en métricas acumuladas antes del modelado, tal y como se describe en el apartado de calidad de los datos.

\subsection{Preparación del dataset de modelado}

El objetivo de esta fase es transformar el dataset de eventos obtenido en el apartado anterior en un conjunto de datos directamente utilizable por los algoritmos de aprendizaje automático. El dataset original contiene una fila por cada acción registrada en los partidos, por lo que debemos convertirlo a un formato que permita representar el estado de cada partido en un minuto determinado. Para ello, se genera una fila por cada par \textit{(partido, minuto)}, de modo que para cada uno de los 3.464 partidos del dataset se generan noventa filas, una por cada minuto del tiempo reglamentario.  La granularidad de un minuto nos permite determinar con mayor precisión a partir de qué instantes del partido el modelo es capaz de predecir el resultado del encuentro, y permite agregar los valores para construir variables de ventana deslizante. El resultado es un dataset de 311.760 registros y 48 variables de entrada.

\subsubsection{Tratamiento del tiempo reglamentario y el tiempo de descuento}

Como se ha visto en el capítulo anterior, el dataset de eventos incluye acciones correspondientes al tiempo reglamentario, al tiempo de descuento y, en los partidos con prórroga, a los periodos adicionales. Para la construcción del dataset de modelado se adoptan las siguientes decisiones:

\begin{itemize}
    \item \textbf{Prórroga y penaltis (periodos 3 a 5):} se excluyen de las variables de entrada. La duración de la prórroga no es fija entre partidos ni entre competiciones, y su estructura difiere del tiempo reglamentario, lo que exigiría un tratamiento diferenciado que añadiría complejidad sin beneficio claro para el objetivo del trabajo. La variable objetivo (\texttt{final\_result}) sí refleja el resultado definitivo del partido, incluido el desenlace en prórroga o penaltis cuando corresponda, de modo que la información sobre el resultado final se preserva íntegramente.

    \item \textbf{Tiempo de descuento (minuto $> 90$, periodo 2):} los eventos registrados en tiempo de descuento representan el 3,2\% del total, tal como se constató en el análisis exploratorio. Dado que el tiempo de descuento varía en función de las interrupciones de cada partido y no tiene una duración homogénea entre encuentros, estos eventos se reasignan al \textit{snapshot} del minuto 90, integrándolos en el estado acumulado de ese minuto. Esta decisión garantiza que cualquier gol marcado en el descuento quede recogido en el estado final del partido sin necesidad de modelar minutos variables más allá de los 90 reglamentarios.
\end{itemize}

\subsubsection{Indicadores de actividad y estado acumulado}

A partir del dataset filtrado se definen 15 indicadores binarios y la variable de \textit{expected goals} (\textit{xG}). Cada tipo de evento del dataset se convierte en un vector binario, de modo que podamos representar el total de acciones de esa clase realizadas por un equipo desde el inicio del partido hasta un minuto $t$.

Los indicadores definidos son: gol, tiro, tiro a puerta, tiro en el área, pase, pase completado, presión defensiva, duelo ganado, despeje, bloqueo, conducción de balón, tarjeta amarilla, tarjeta roja, y entrada en el tercio de ataque. El \textit{xG} se trata como variable continua y toma el valor 0 para todos los eventos que no corresponden a un tiro.

El estado acumulado de las variables se calcula aplicando una suma acumulativa sobre los eventos ordenados por minuto dentro de cada grupo \textit{(partido, equipo)}. Para rellenar los minutos en los que no se registra ningún evento, se usarán los valores anteriores mediante propagación hacia adelante (\textit{forward fill}), lo que equivale a mantener el estado del partido sin cambios hasta que se produzca una nueva acción. Los minutos anteriores al primer evento del partido se inicializan a cero.

\subsubsection{Variables de ventana deslizante}

Las variables acumuladas desde el inicio del partido capturan el estado global del encuentro, pero no distinguen si una acción tuvo lugar en el minuto 5 o en el 85. Para medir la intensidad de juego reciente e incorporar una dimensión de \textit{momentum}, se calculan tres variables de ventana deslizante de 15 minutos: el \textit{xG} generado en los últimos 15 minutos (\texttt{xg\_last15}), el número de tiros en ese mismo intervalo (\texttt{shots\_last15}) y el número de presiones defensivas (\texttt{pressures\_last15}). Estas variables se obtienen eficientemente como la diferencia entre el valor acumulado en el minuto $t$ y el valor acumulado en el minuto $t - 15$, que se extrae desplazando la serie temporal 15 posiciones hacia adelante dentro de cada grupo \textit{(partido, equipo)}.

La selección de \textit{xG}, tiros y presiones para las variables de ventana responde a los resultados del análisis exploratorio, donde estas métricas presentaron las mayores correlaciones con el resultado final. Extender la ventana al conjunto completo de indicadores añadiría 30 variables con un coste computacional significativo y una contribución marginal previsiblemente reducida, dado que las tres seleccionadas cubren las dimensiones ofensiva (\textit{xG} y tiros) y defensiva (presiones) del juego reciente.

\subsubsection{Variables derivadas y \textit{feature engineering}}

Una vez disponibles las estadísticas acumuladas y las variables de ventana para ambos equipos, se construye la representación definitiva del estado del partido, representando ambos equipos en una única fila por \textit{(partido, minuto)}, duplicando las columnas y añadiendo sufijos \texttt{\_home} y \texttt{\_away} que permitan representar el equipo al que corresponden. Además, se generan unas variables o \textit{features} que, aunque no aportan nueva información, permiten transformar la semántica de las variables y su efecto en la calibración de los modelos. Las variables derivadas construidas son las siguientes:

\begin{itemize}
    \item \textbf{Diferenciales del marcador} (\texttt{score\_diff}, \texttt{xg\_diff}, \texttt{shots\_diff}): diferencia entre el valor del equipo local y el del visitante para los goles, el \textit{xG} acumulado y el número de tiros. Sintetizan en un único valor la ventaja o desventaja relativa de cada equipo en cada dimensión, evitando que el modelo tenga que aprender implícitamente esta operación de resta.

    \item \textbf{Posesión del balón} (\texttt{possession\_home}): proporción de conducciones de balón realizadas por el equipo local sobre el total de conducciones del partido. StatsBomb no proporciona una métrica directa de posesión, y las conducciones (\textit{carries}), que registran el desplazamiento físico del balón por parte de un jugador, constituyen la aproximación más precisa disponible en el dataset. En los minutos previos al primer evento registrado se asigna un valor de 0,5 como posesión neutra.

    \item \textbf{Tasa de pase completado} (\texttt{pass\_completion\_home}, \texttt{pass\_completion\_away}): cociente entre los pases completados y los pases intentados para cada equipo. Captura la calidad y el control en la circulación del balón, que el análisis exploratorio confirmó que correlaciona positivamente con la victoria, con una diferencia de aproximadamente cuatro puntos porcentuales entre equipos ganadores y perdedores.

    \item \textbf{Total de goles} (\texttt{total\_goals}): suma de los goles marcados por ambos equipos hasta el minuto $t$. Un partido con marcador 3-3 presenta un perfil dinámico muy distinto a uno con 0-0, aun cuando \texttt{score\_diff} sea cero en ambos casos. Esta variable permite al modelo distinguir entre partidos abiertos, con muchos goles y mayor probabilidad de nuevas anotaciones, y partidos cerrados de carácter más defensivo.

    \item \textbf{Calidad ofensiva por tiro} (\texttt{xg\_per\_shot\_home}, \texttt{xg\_per\_shot\_away}): cociente entre el \textit{xG} acumulado y el número de tiros realizados. Diferencia entre equipos que generan ocasiones de alta peligrosidad, principalmente desde dentro del área, y aquellos con un volumen elevado de tiros de escasa probabilidad de gol. En los minutos anteriores al primer tiro del equipo se asigna el valor cero.

    \item \textbf{Sobrerendimiento respecto al xG} (\texttt{goals\_minus\_xg\_home}, \texttt{goals\_minus\_xg\_away}): diferencia entre los goles marcados y el \textit{xG} acumulado. Un valor positivo indica que el equipo está materializando más goles de los que cabría esperar a partir de la calidad de sus ocasiones, lo que puede responder tanto a una finalización de alta efectividad como a factores de varianza aleatoria. Un valor negativo refleja la situación contraria, señalando que el equipo está creando peligro sin traducirlo en goles.

    \item \textbf{Tipo de competición} (\texttt{is\_womens}): indicador binario que distingue las competiciones femeninas de las masculinas. El análisis exploratorio reveló diferencias sistemáticas en la distribución de resultados entre ambos contextos: los empates representan el 16,7\,\% de los partidos femeninos frente al 24,2\,\% en los masculinos, y las victorias visitantes alcanzan el 36,1\,\% en el fútbol femenino frente al 31,0\,\% en el masculino. Estas diferencias justifican incluir esta variable para que el modelo pueda calibrar sus probabilidades en función del tipo de competición.
\end{itemize}

\subsubsection{Composición final del dataset de modelado}

El dataset resultante contiene 311.760 filas, correspondientes a los 3.464 partidos del conjunto de datos y sus 90 minutos reglamentarios, y 48 variables de entrada agrupadas en ocho categorías. La tabla~\ref{tab:features} resume la composición del dataset. La integridad del conjunto fue verificada mediante tres comprobaciones: ausencia de valores ausentes en las columnas clave para el modelado, coincidencia entre el marcador derivado y el marcador real de un partido de muestra seleccionado aleatoriamente, y correspondencia exacta entre el número de filas generadas y el producto del número de partidos por el número de minutos.

\begin{table}[H]
    \centering
    \begin{tabular}{lcp{7.5cm}}
        \hline
        \textbf{Categoría}           & \textbf{Variables} & \textbf{Descripción}                                                                                                                                                                                                                                     \\
        \hline
        Estadísticas acumuladas      & 28                 & Goles, tiros, tiros a puerta, tiros en el área, pases, presiones, duelos ganados, despejes, bloqueos, conducciones, tarjetas amarillas, tarjetas rojas, entradas en el tercio de ataque y \textit{xG}; para el equipo local y el visitante por separado. \\[4pt]
        Ventana deslizante (15\,min) & 6                  & \textit{xG}, tiros y presiones en los últimos 15 minutos; para el equipo local y el visitante.                                                                                                                                                           \\[4pt]
        Diferenciales                & 3                  & Diferencia local-visitante de goles (\texttt{score\_diff}), \textit{xG} (\texttt{xg\_diff}) y tiros (\texttt{shots\_diff}).                                                                                                                              \\[4pt]
        Control y posesión           & 4                  & Posesión del local, tasa de pase completado de ambos equipos y total de goles.                                                                                                                                                                           \\[4pt]
        Calidad ofensiva             & 4                  & \textit{xG} por tiro y sobrerendimiento respecto al \textit{xG}; para ambos equipos.                                                                                                                                                                     \\[4pt]
        Superioridad numérica        & 1                  & Diferencial de jugadores en campo (\texttt{players\_diff}).                                                                                                                                                                                              \\[4pt]
        Tiempo restante              & 1                  & Minutos restantes hasta el final del tiempo reglamentario.                                                                                                                                                                                               \\[4pt]
        Tipo de competición          & 1                  & Indicador binario de competición femenina (\texttt{is\_womens}).                                                                                                                                                                                         \\
        \hline
        \textbf{Total}               & \textbf{48}        &                                                                                                                                                                                                                                                          \\
        \hline
        \multicolumn{3}{l}{\small Fuente: elaboración propia.}
    \end{tabular}
    \caption{Categorías de variables del dataset de modelado}
    \label{tab:features}
\end{table}
\section{Modelado y comparativa}

\subsection{Planteamiento de la comparativa}

Los modelos se evalúan sobre el conjunto de test formado por las temporadas 2020-2025, con 692 partidos y 62.280 filas. Las tres métricas principales son el \textit{accuracy} global, el F1-score macro y la pérdida logarítmica (\textit{log-loss}). El F1-score macro promedia el F1 de las tres clases con el mismo peso, lo que penaliza por igual los errores en las clases minoritarias. La pérdida logarítmica evalúa la calidad de las probabilidades predichas, no solo la clase asignada.

Adicionalmente, se calcula el \textit{accuracy} en seis instantes del partido: los minutos 15, 30, 45, 60, 75 y 90. Este análisis temporal permite cuantificar a partir de qué momento el modelo supera el umbral de referencia del 70\,\% y cómo evoluciona la confianza de la predicción conforme avanza el encuentro.

La línea base del problema es predecir siempre la clase mayoritaria (victoria local), con un 42,9\,\% de \textit{accuracy}. Cualquier modelo útil debe superar este valor de forma sustancial.

\subsubsection{Justificación de la división temporal}

Para evaluar los modelos se utiliza una división temporal de los datos a nivel de partido. Esta decisión se toma porque el objetivo del sistema no es memorizar patrones de partidos ya vistos, sino comprobar si el modelo es capaz de generalizar a partidos posteriores en el tiempo. Por este motivo, los partidos más antiguos se utilizan para entrenamiento y los partidos más recientes se reservan para test. Además, la separación se realiza por \texttt{match\_id} y no por filas individuales, ya que cada partido genera una fila por minuto. Si se dividiera el dataset de forma aleatoria por filas, podrían aparecer minutos de un mismo partido tanto en entrenamiento como en test, provocando una fuga de información. De esta forma, la estrategia de validación temporal permite simular un escenario más realista, en el que el modelo se entrena con información histórica y se evalúa sobre encuentros no utilizados durante el entrenamiento.

Aún así, esta estrategia también presenta alguna limitación: la distribución de los datos puede cambiar con el paso del tiempo. Las competiciones disponibles, las temporadas incluidas, el estilo de juego o la proporción de resultados pueden variar entre el conjunto de entrenamiento y el conjunto de test. Este fenómeno, conocido como deriva temporal de los datos, puede afectar al rendimiento del modelo si los partidos recientes no siguen exactamente la misma distribución que los partidos históricos. Por ello, la interpretación de los resultados debe tener en cuenta que el test temporal es más exigente que una partición aleatoria, pero también más adecuado para valorar la capacidad real de generalización del sistema.

\subsection{Modelos seleccionados}

Se entrenan y comparan tres clasificadores: Random Forest, XGBoost y SVM lineal.

\textbf{Random Forest} construye un conjunto de árboles de decisión sobre submuestras aleatorias del dataset y combina sus predicciones por votación mayoritaria. No requiere normalización de variables, gestiona bien las interacciones entre variables sin configuración adicional y permite interpretar el modelo mediante la importancia de cada variable. Se utiliza como modelo de referencia.

\textbf{XGBoost} es un método de potenciación del gradiente que construye los árboles de forma secuencial. Cada árbol corrige los errores del árbol anterior minimizando una función de pérdida regularizada. En clasificación sobre datos tabulares estructurados, este método suele superar al Random Forest gracias a su mayor capacidad de ajuste y a la regularización incorporada. Se incluye como uno de los modelos principales de la comparativa, especialmente por su buen rendimiento en problemas tabulares y por sus mecanismos internos de regularización.

El \textbf{SVM lineal} se incluye como un tercer modelo de comparación. Este tipo de modelo busca separar las clases mediante márgenes de decisión, por lo que puede resultar útil para comprobar si el problema puede resolverse adecuadamente con una frontera de decisión más simple. En este trabajo se utiliza una versión lineal del SVM, ya que el dataset contiene un número elevado de filas y un SVM con kernel no lineal tendría un coste computacional mucho mayor. A diferencia de Random Forest y XGBoost, este modelo sí requiere normalizar las variables de entrada antes del entrenamiento.


Se consideró también el uso de una red neuronal recurrente (LSTM) para aprovechar la naturaleza secuencial de las series minuto a minuto, pero fue descartada por la complejidad adicional de diseño y validación que implicaría.

\subsection{Ajuste de hiperparámetros}

\subsubsection{Random Forest}

Para Random Forest se aplicó una búsqueda aleatoria de hiperparámetros mediante \texttt{RandomizedSearchCV}. La búsqueda se realizó únicamente sobre el conjunto de entrenamiento, manteniendo intacto el conjunto de test para la evaluación final.

Con el objetivo de evitar fuga de información, la validación interna se definió a nivel de partido mediante \texttt{match\_id}. Para ello, se utilizaron 2.217 partidos para ajustar cada configuración candidata y 555 partidos para la validación interna. De esta forma, todos los minutos de un mismo encuentro permanecen siempre dentro de la misma partición.

El espacio de búsqueda incluyó distintos valores para el número de árboles, la profundidad máxima, el número mínimo de muestras por hoja, el número de variables consideradas en cada división y la estrategia de ponderación de clases. En total se evaluaron 12 configuraciones candidatas, seleccionando la mejor según el F1-score macro sobre la partición interna de validación. Esta métrica se utilizó como criterio principal porque permite valorar de forma equilibrada el rendimiento entre victoria local, empate y victoria visitante.

La mejor configuración seleccionada fue un Random Forest con 500 árboles, profundidad máxima de 8, un mínimo de 5 muestras por hoja, selección de variables mediante \texttt{sqrt} y pesos de clase balanceados. Esta configuración obtuvo en validación interna un accuracy del 64,8 \%, un F1-score macro del 62,6 \% y una pérdida logarítmica de 0,771. La configuración seleccionada corresponde a un modelo más regularizado, con árboles menos profundos, lo que favorece un comportamiento más equilibrado entre clases.

\subsubsection{XGBoost}

Para XGBoost también se aplicó una búsqueda aleatoria de hiperparámetros mediante \texttt{RandomizedSearchCV}.

La búsqueda se realizó exclusivamente dentro del conjunto de entrenamiento. Al igual que en Random Forest, se utilizó una partición interna a nivel de partido mediante \texttt{match\_id}, con 2.217 partidos para ajustar cada configuración candidata y 555 partidos para la validación interna. Esta estrategia evita que diferentes minutos de un mismo encuentro aparezcan simultáneamente en entrenamiento interno y validación interna.

El espacio de búsqueda incluyó hiperparámetros relacionados con la complejidad del modelo, la tasa de aprendizaje, el submuestreo y la regularización. En concreto, se evaluaron diferentes valores para el número de estimadores, la profundidad máxima de los árboles, la tasa de aprendizaje, \texttt{subsample}, \texttt{colsample\_bytree}, \texttt{min\_child\_weight}, \texttt{reg\_lambda}, \texttt{reg\_alpha} y \texttt{gamma}. En total se probaron 12 configuraciones candidatas, seleccionando la mejor según el F1-score macro sobre la partición interna de validación.

La configuración seleccionada utiliza 300 estimadores, profundidad máxima de 5, tasa de aprendizaje de 0,03, \texttt{subsample} de 0,7, \texttt{colsample\_bytree} de 0,9, \texttt{min\_child\_weight} igual a 3, \texttt{reg\_lambda} igual a 3, \texttt{reg\_alpha} igual a 0,1 y \texttt{gamma} igual a 0,5. Esta configuración obtuvo en validación interna un accuracy del 62,2 \%, un F1-score macro del 62,0 \% y una pérdida logarítmica de 0,783. El resultado refleja una configuración regularizada, con tasa de aprendizaje baja y mecanismos de submuestreo, orientada a mejorar la generalización del modelo.

\subsubsection{SVM lineal}

Para el SVM se utiliza un flujo ligeramente distinto, ya que este modelo es sensible a la escala de las variables y no proporciona probabilidades directamente. En primer lugar, las variables de entrada se normalizan mediante \texttt{StandardScaler}. Después, se evalúan distintos valores del parámetro \texttt{C}, que controla el equilibrio entre margen amplio y penalización de errores de clasificación. Los valores probados son 0,01, 0,1, 1, 10 y 100, seleccionando el mejor valor según el F1-score macro en una partición interna de validación.

La división interna utilizada para el SVM se realiza únicamente dentro del conjunto de entrenamiento, manteniendo intacto el conjunto de test empleado por el resto de modelos. Esta división separa los datos en tres subconjuntos: uno para ajustar el modelo, otro para seleccionar el valor de \texttt{C} y otro para calibrar las probabilidades. La calibración se realiza porque \texttt{LinearSVC} no genera probabilidades de forma directa, pero estas son necesarias para calcular la pérdida logarítmica y comparar el modelo con Random Forest y XGBoost bajo las mismas métricas.

El mejor valor obtenido fue \texttt{C = 0.01}, lo que indica que una configuración con mayor regularización fue la más adecuada para este modelo.

\subsection{Resultados comparativos}

\subsubsection{Random Forest}

El Random Forest obtiene un accuracy global del 65,0 \%, un F1-score macro del 63,3 \% y una pérdida logarítmica de 0,772 sobre el conjunto de test. Estos resultados muestran que el modelo mantiene un rendimiento sólido en la predicción del resultado final del partido, aunque su comportamiento debe interpretarse de forma conjunta entre las distintas métricas. Mientras que la accuracy global se sitúa ligeramente por debajo de otros modelos basados en árboles, el F1-score macro refleja un comportamiento más equilibrado entre las tres clases.

La tabla \ref{tab:rf_por_minuto} recoge el accuracy y el F1-score macro para los seis puntos de corte evaluados.

\begin{table}[H]
    \centering
    \begin{tabular}{ccc}
        \hline
        \textbf{Minuto} & \textbf{Accuracy} & \textbf{Macro-F1} \\
        \hline
        15              & 52,5 \%           & 49,1 \%           \\
        30              & 55,9 \%           & 54,4 \%           \\
        45              & 62,7 \%           & 61,8 \%           \\
        60              & 70,4 \%           & 69,3 \%           \\
        75              & 77,5 \%           & 76,2 \%           \\
        90              & 93,1 \%           & 92,5 \%           \\
        \hline
    \end{tabular}
    \caption{Accuracy y Macro-F1 del Random Forest por minuto de partido}
    \label{tab:rf_por_minuto}
\end{table}

El análisis temporal muestra que el rendimiento del Random Forest aumenta de forma progresiva a medida que avanza el partido. En el minuto 15, el modelo obtiene un accuracy del 52,5 \% y un F1-score macro del 49,1 \%, lo que refleja la dificultad de anticipar el resultado final cuando todavía existe poca información acumulada. En este tramo inicial, las diferencias entre equipos suelen ser reducidas y muchas variables relevantes, como el marcador o la producción ofensiva acumulada, aún no presentan señales suficientemente claras.

A partir del minuto 30, el rendimiento empieza a mejorar de forma gradual. En el minuto 45, el modelo alcanza un accuracy del 62,7 \% y un F1-score macro del 61,8 \%, lo que indica que la información generada durante la primera parte ya permite diferenciar mejor entre los posibles resultados finales. En el minuto 60, el modelo supera el 70 \% de accuracy, con un valor del 70,4 \%, y alcanza un F1-score macro del 69,3 \%. Posteriormente, en el minuto 75, el rendimiento aumenta hasta un 77,5 \% de accuracy y un 76,2 \% de F1-score macro. Finalmente, en el minuto 90, el modelo obtiene un accuracy del 93,1 \% y un F1-score macro del 92,5 \%, debido a que el marcador y las métricas acumuladas representan ya de forma muy directa el desenlace del partido.

Esta evolución se observa en la figura \ref{fig:rf_minutos}, donde se aprecia un crecimiento sostenido tanto del accuracy como del F1-score macro conforme se incorpora más información del desarrollo del encuentro.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{assets/figuras/modeling/accuracy_por_minuto_rf.pdf}
    \caption{Evolución del accuracy y el Macro-F1 por minuto de partido para el Random Forest}
    \label{fig:rf_minutos}
\end{figure}

La matriz de confusión normalizada, mostrada en la figura \ref{fig:rf_confusion}, permite analizar el comportamiento del modelo para cada una de las clases. El Random Forest identifica correctamente el 68 \% de las victorias locales y el 70 \% de las victorias visitantes. En ambos casos, el modelo mantiene una capacidad razonable para detectar situaciones en las que uno de los equipos termina imponiéndose.

El resultado más relevante aparece en la clase empate, donde el modelo alcanza un recall del 52 \% y un F1-score del 47 \%. Aunque esta sigue siendo la clase más difícil de predecir, el modelo consigue reconocer una proporción importante de empates. Este comportamiento es coherente con la selección de hiperparámetros mediante F1-score macro, ya que la configuración final no maximiza únicamente la accuracy global, sino que busca un rendimiento más equilibrado entre las tres clases.

No obstante, el aumento en la detección de empates también implica una mayor confusión con algunas situaciones de victoria local o visitante. Esto explica que la accuracy global no sea necesariamente la métrica más alta, mientras que el F1-score macro mejora al repartir mejor el rendimiento entre clases. En un problema como la predicción de resultados de fútbol, esta diferencia es relevante, ya que el empate es una clase menos frecuente y más difícil de separar de los marcadores ajustados.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{assets/figuras/modeling/confusion_rf.pdf}
    \caption{Matriz de confusión normalizada del Random Forest sobre el conjunto de test}
    \label{fig:rf_confusion}
\end{figure}

El análisis de importancia de variables, mostrado en la figura \ref{fig:rf_importancia}, indica que la variable más relevante es la diferencia de goles, seguida de los goles acumulados del equipo local y visitante. También aparecen entre las variables principales la diferencia de xG, la diferencia entre goles y xG para ambos equipos, el total de goles, la posesión local, la precisión de pase del equipo local y el xG acumulado del equipo visitante.

Estos resultados son coherentes con el problema planteado. El modelo se apoya principalmente en el estado del marcador, pero también incorpora variables relacionadas con la calidad ofensiva generada, el rendimiento respecto a los goles esperados y el control del juego. La presencia de variables como \texttt{xg\_diff}, \texttt{goals\_minus\_xg\_home}, \texttt{goals\_minus\_xg\_away} y \texttt{possession\_home} indica que el Random Forest no utiliza únicamente el marcador actual, sino también información contextual sobre cómo se ha desarrollado el partido.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{assets/figuras/modeling/feature_importance_rf.pdf}
    \caption{Importancia de las 20 variables más relevantes en el Random Forest}
    \label{fig:rf_importancia}
\end{figure}

De forma complementaria, se calculó la Permutation Importance del Random Forest sobre una muestra de 3.000 filas del conjunto de test, utilizando el F1-score macro como métrica de referencia. Este análisis mide cuánto disminuye el rendimiento del modelo cuando se altera aleatoriamente cada variable. Como se observa en la figura \ref{fig:perm_rf}, \texttt{score\_diff} es la variable con mayor impacto sobre el rendimiento, seguida de variables como \texttt{possession\_home}, \texttt{goals\_away}, \texttt{total\_goals} y \texttt{pass\_completion\_away}. Esto confirma que el modelo combina información del marcador con variables relacionadas con el control del juego y el rendimiento acumulado.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{assets/figuras/modeling/permutation_importance_rf.pdf}
    \caption{Permutation Importance del Random Forest sobre una muestra del conjunto de test}
    \label{fig:perm_rf}
\end{figure}

En conjunto, los resultados del Random Forest muestran que el modelo es capaz de aprovechar de forma efectiva las variables acumuladas minuto a minuto. La búsqueda aleatoria de hiperparámetros permite seleccionar una configuración más regularizada y equilibrada, que mejora especialmente la identificación de empates. Aun así, el modelo mantiene la dificultad propia del problema: distinguir correctamente partidos igualados sin aumentar en exceso la confusión con victorias locales o visitantes.

\paragraph{Evaluación de ejemplos individuales:}

Para evaluar el comportamiento del Random Forest en casos concretos, se seleccionaron tres partidos aleatorios del conjunto de test y se analizaron las probabilidades generadas en los minutos 30, 60 y 90. Los tres partidos seleccionados finalizaron con victoria local, por lo que se comprueba si el modelo es capaz de anticipar correctamente este resultado en distintos momentos del encuentro.

\begin{table}[H]
    \centering
    \resizebox{\textwidth}{!}{
        \begin{tabular}{lcccccc}
            \hline
            \textbf{match\_id} & \textbf{Minuto} & \textbf{P(local)} & \textbf{P(empate)} & \textbf{P(visitante)} & \textbf{Predicción} & \textbf{Acierto} \\
            \hline
            3775617            & 30              & 45,64 \%          & 35,46 \%           & 18,89 \%              & Local               & Sí               \\
            3775617            & 60              & 41,78 \%          & 38,02 \%           & 20,19 \%              & Local               & Sí               \\
            3775617            & 90              & 47,01 \%          & 38,02 \%           & 14,96 \%              & Local               & Sí               \\
            3893811            & 30              & 74,11 \%          & 19,32 \%           & 6,57 \%               & Local               & Sí               \\
            3893811            & 60              & 74,70 \%          & 20,79 \%           & 4,51 \%               & Local               & Sí               \\
            3893811            & 90              & 74,90 \%          & 21,03 \%           & 4,07 \%               & Local               & Sí               \\
            3920392            & 30              & 32,98 \%          & 40,55 \%           & 26,47 \%              & Empate              & No               \\
            3920392            & 60              & 26,54 \%          & 50,35 \%           & 23,11 \%              & Empate              & No               \\
            3920392            & 90              & 71,52 \%          & 23,04 \%           & 5,44 \%               & Local               & Sí               \\
            \hline
        \end{tabular}
    }
    \caption{Evolución de las probabilidades predichas por Random Forest en tres partidos del conjunto de test}
    \label{tab:rf_ejemplos_evolucion}
\end{table}

El Random Forest acierta 7 de las 9 predicciones analizadas. En los dos primeros partidos, el modelo predice victoria local en los tres minutos, aunque en el partido 3775617 las probabilidades son más ajustadas, especialmente en los minutos 60 y 90. En el partido 3893811, la predicción es más estable y la probabilidad de victoria local se mantiene alrededor del 75 \%, lo que refleja una señal más clara para el modelo. En cambio, en el partido 3920392 el encuentro permanece empatado en los minutos 30 y 60, por lo que el modelo asigna mayor probabilidad al empate. Cuando el marcador pasa a ser favorable al equipo local en el minuto 90, el modelo corrige la predicción y asigna un 71,52 \% de probabilidad a la victoria local. Este comportamiento es coherente con la importancia de variables como \texttt{score\_diff}, \texttt{total\_goals} y las métricas acumuladas de rendimiento.

\subsubsection{XGBoost}

El modelo XGBoost obtiene un accuracy global del 65,4 \%, un F1-score macro del 62,7 \% y una pérdida logarítmica de 0,753 sobre el conjunto de test. Estos resultados muestran que el modelo mantiene un rendimiento competitivo en la predicción del resultado final del partido.

La tabla \ref{tab:xgb_por_minuto} recoge el accuracy y el F1-score macro para los seis puntos de corte evaluados.

\begin{table}[H]
    \centering
    \begin{tabular}{ccc}
        \hline
        \textbf{Minuto} & \textbf{Accuracy} & \textbf{Macro-F1} \\
        \hline
        15              & 53,6 \%           & 48,1 \%           \\
        30              & 59,8 \%           & 55,9 \%           \\
        45              & 63,3 \%           & 60,3 \%           \\
        60              & 68,8 \%           & 67,0 \%           \\
        75              & 76,6 \%           & 75,1 \%           \\
        90              & 92,5 \%           & 91,8 \%           \\
        \hline
    \end{tabular}
    \caption{Accuracy y Macro-F1 del XGBoost por minuto de partido}
    \label{tab:xgb_por_minuto}
\end{table}

El rendimiento temporal del XGBoost muestra una evolución progresiva a medida que avanza el partido. En el minuto 15, el modelo obtiene un accuracy del 53,6 \% y un F1-score macro del 48,1 \%, valores que reflejan la dificultad de anticipar el resultado final en los primeros compases del encuentro. En ese momento, el modelo dispone de poca información acumulada y muchas de las variables más relevantes todavía no presentan diferencias claras entre los equipos.

A partir del minuto 30, el rendimiento empieza a mejorar de forma más consistente. En el minuto 45, el modelo alcanza un accuracy del 63,3 \% y un F1-score macro del 60,3 \%, lo que indica que la información de la primera parte ya aporta señales relevantes para la predicción. En el minuto 60, el accuracy se sitúa en el 68,8 \% y el F1-score macro en el 67,0 \%, acercándose al umbral del 70 \%. Posteriormente, en el minuto 75, el modelo alcanza un accuracy del 76,6 \% y un F1-score macro del 75,1 \%. Finalmente, en el minuto 90, obtiene un accuracy del 92,5 \% y un F1-score macro del 91,8 \%, debido a que el estado del marcador y las métricas acumuladas representan ya de forma muy directa el desenlace final del partido.

Esta evolución se muestra en la figura \ref{fig:xgb_minutos}, donde se observa cómo la capacidad predictiva del modelo aumenta conforme se dispone de más información del desarrollo del encuentro.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{assets/figuras/modeling/accuracy_por_minuto_xgb.pdf}
    \caption{Evolución del accuracy y el Macro-F1 por minuto de partido para el XGBoost}
    \label{fig:xgb_minutos}
\end{figure}

La matriz de confusión normalizada, mostrada en la figura \ref{fig:xgb_confusion}, permite analizar el comportamiento del modelo para cada una de las clases. El XGBoost identifica correctamente el 72 \% de las victorias locales y el 72 \% de las victorias visitantes. Esto indica que el modelo mantiene un comportamiento equilibrado en la detección de partidos que terminan con victoria de alguno de los dos equipos.

La clase empate continúa siendo la más difícil de predecir. Para esta clase, el modelo obtiene un recall del 44 \% y un F1-score del 45 \%. Este resultado es coherente con la naturaleza del problema, ya que el empate suele corresponder a situaciones de mayor equilibrio entre equipos y puede transformarse en victoria local o visitante por un único evento decisivo. Aun así, el modelo consigue identificar una parte relevante de los empates, lo que contribuye a mantener un F1-score macro competitivo.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{assets/figuras/modeling/confusion_xgb.pdf}
    \caption{Matriz de confusión normalizada del XGBoost sobre el conjunto de test}
    \label{fig:xgb_confusion}
\end{figure}

El análisis de importancia de variables, mostrado en la figura \ref{fig:xgb_importancia}, permite identificar qué factores tienen mayor peso en las decisiones del modelo. La variable más relevante es la diferencia de goles, seguida de los goles acumulados del equipo local, las entradas en el tercio ofensivo del equipo visitante, los minutos restantes, los pases del equipo local y los goles acumulados del equipo visitante. También aparecen entre las variables más importantes la posesión local, los tiros del equipo visitante, el total de goles y los pases del equipo visitante.

Estos resultados muestran que el XGBoost combina información del marcador con variables que describen el contexto temporal y el desarrollo del juego. La presencia de \texttt{score\_diff}, \texttt{goals\_home} y \texttt{goals\_away} confirma el peso del marcador actual, mientras que variables como \texttt{minutes\_remaining}, \texttt{attacks\_third\_away}, \texttt{passes\_home}, \texttt{shots\_away} y \texttt{possession\_home} indican que el modelo también incorpora señales relacionadas con el tiempo restante, el dominio territorial, la circulación de balón y la producción ofensiva.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{assets/figuras/modeling/feature_importance_xgb.pdf}
    \caption{Importancia de las 20 variables más relevantes en el XGBoost}
    \label{fig:xgb_importancia}
\end{figure}

En el caso de XGBoost, la Permutation Importance también identifica \texttt{score\_diff} como la variable más determinante, con una diferencia clara respecto al resto de variables. Tal como se muestra en la figura \ref{fig:perm_xgb}, también destacan \texttt{possession\_home}, \texttt{attacks\_third\_away}, \texttt{pass\_completion\_away} y \texttt{minutes\_remaining}. Estos resultados indican que el modelo se apoya principalmente en el estado del marcador, pero también incorpora información sobre el tiempo restante, el dominio territorial y la circulación de balón.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{assets/figuras/modeling/permutation_importance_xgb.pdf}
    \caption{Permutation Importance del XGBoost sobre una muestra del conjunto de test}
    \label{fig:perm_xgb}
\end{figure}

En conjunto, los resultados del XGBoost muestran que el modelo aprovecha de forma efectiva las variables acumuladas minuto a minuto y mantiene un rendimiento competitivo frente al resto de modelos. Su principal fortaleza se encuentra en combinar señales del marcador, el tiempo restante y el desarrollo ofensivo del partido. No obstante, al igual que ocurre con el resto de enfoques, la predicción de empates sigue siendo el principal reto del problema.

\paragraph{Evaluación de ejemplos individuales:}

Para XGBoost se utilizaron los mismos tres partidos y los mismos minutos que en el análisis anterior, con el objetivo de comparar las predicciones de los modelos bajo las mismas condiciones. En todos los casos, el resultado final real fue victoria local.

\begin{table}[H]
    \centering
    \resizebox{\textwidth}{!}{
        \begin{tabular}{lcccccc}
            \hline
            \textbf{match\_id} & \textbf{Minuto} & \textbf{P(local)} & \textbf{P(empate)} & \textbf{P(visitante)} & \textbf{Predicción} & \textbf{Acierto} \\
            \hline
            3775617            & 30              & 43,12 \%          & 33,34 \%           & 23,55 \%              & Local               & Sí               \\
            3775617            & 60              & 32,90 \%          & 36,11 \%           & 30,99 \%              & Empate              & No               \\
            3775617            & 90              & 61,79 \%          & 32,84 \%           & 5,37 \%               & Local               & Sí               \\
            3893811            & 30              & 85,07 \%          & 10,98 \%           & 3,95 \%               & Local               & Sí               \\
            3893811            & 60              & 80,22 \%          & 15,96 \%           & 3,82 \%               & Local               & Sí               \\
            3893811            & 90              & 86,45 \%          & 11,24 \%           & 2,32 \%               & Local               & Sí               \\
            3920392            & 30              & 33,92 \%          & 40,33 \%           & 25,75 \%              & Empate              & No               \\
            3920392            & 60              & 22,73 \%          & 56,71 \%           & 20,56 \%              & Empate              & No               \\
            3920392            & 90              & 90,97 \%          & 8,18 \%            & 0,85 \%               & Local               & Sí               \\
            \hline
        \end{tabular}
    }
    \caption{Evolución de las probabilidades predichas por XGBoost en tres partidos del conjunto de test}
    \label{tab:xgb_ejemplos_evolucion}
\end{table}

XGBoost acierta 6 de las 9 predicciones analizadas. Su comportamiento es especialmente sólido en el partido 3893811, donde asigna una probabilidad elevada a la victoria local desde el minuto 30 y la mantiene durante todo el encuentro. En el partido 3775617, el modelo predice correctamente victoria local en los minutos 30 y 90, pero en el minuto 60 asigna una probabilidad ligeramente superior al empate, lo que refleja un estado de partido más incierto. En el partido 3920392, XGBoost predice empate en los minutos 30 y 60, cuando el marcador todavía no favorece al equipo local. Sin embargo, en el minuto 90 la probabilidad de victoria local aumenta hasta el 90,97 \%, corrigiendo la predicción cuando el marcador y las métricas acumuladas ya apuntan claramente al resultado final. Este comportamiento confirma que XGBoost reacciona de forma marcada a los cambios en el estado del partido.

\subsubsection{SVM lineal}

El SVM lineal obtiene un accuracy global del 63,9 \%, un F1-score macro del 50,0 \% y una pérdida logarítmica de 0,798 sobre el conjunto de test. A diferencia de los modelos basados en árboles, este clasificador utiliza una frontera de decisión lineal, por lo que su rendimiento permite valorar hasta qué punto el problema puede resolverse mediante una separación más simple entre clases. Aunque la accuracy global se mantiene en un nivel aceptable, el F1-score macro es considerablemente más bajo, lo que anticipa un comportamiento desigual entre las tres clases.

La tabla \ref{tab:svm_por_minuto} recoge el accuracy y el F1-score macro para los seis puntos de corte evaluados.

\begin{table}[H]
    \centering
    \begin{tabular}{ccc}
        \hline
        \textbf{Minuto} & \textbf{Accuracy} & \textbf{Macro-F1} \\
        \hline
        15              & 56,6 \%           & 42,5 \%           \\
        30              & 60,0 \%           & 45,9 \%           \\
        45              & 63,0 \%           & 48,6 \%           \\
        60              & 68,4 \%           & 54,2 \%           \\
        75              & 70,8 \%           & 56,6 \%           \\
        90              & 77,2 \%           & 65,7 \%           \\
        \hline
    \end{tabular}
    \caption{Accuracy y Macro-F1 del SVM lineal por minuto de partido}
    \label{tab:svm_por_minuto}
\end{table}

El análisis por minuto muestra que el SVM lineal también aprovecha la información acumulada durante el partido, aunque de forma menos marcada. En el minuto 15 obtiene un accuracy del 56,6 \%, pero un F1-score macro de solo 42,5 \%, lo que indica que desde los primeros tramos ya existe un desequilibrio entre clases. A medida que avanza el encuentro, la accuracy aumenta de forma progresiva y supera el 70 \% en el minuto 75. Sin embargo, el F1-score macro crece con más lentitud y alcanza únicamente el 65,7 \% en el minuto 90.

Esta diferencia entre accuracy y F1-score macro indica que el modelo acierta una parte relevante de los casos, pero no distribuye sus aciertos de forma equilibrada entre las tres clases. Por tanto, la evolución temporal confirma que el SVM lineal utiliza la información acumulada del partido, pero su capacidad para representar la frontera entre victoria local, empate y victoria visitante es más limitada. La figura \ref{fig:svm_minutos} muestra esta evolución.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{assets/figuras/modeling/accuracy_por_minuto_svm.pdf}
    \caption{Evolución del accuracy y el Macro-F1 por minuto de partido para el SVM lineal}
    \label{fig:svm_minutos}
\end{figure}

La matriz de confusión normalizada, mostrada en la figura \ref{fig:svm_confusion}, permite identificar con claridad la principal limitación del modelo. El SVM lineal reconoce correctamente la mayoría de victorias locales, con un recall del 87 \%, y también una proporción elevada de victorias visitantes, con un recall del 78 \%. Sin embargo, apenas identifica correctamente los empates, cuyo recall se reduce al 3,3 \%.

Este comportamiento muestra que el modelo tiende a asignar gran parte de los partidos empatados a alguna de las dos clases de victoria. En términos prácticos, el SVM lineal separa razonablemente bien situaciones en las que uno de los equipos termina ganando, pero no consigue delimitar una zona clara para los partidos que finalizan en empate. Esta limitación es especialmente relevante porque el empate no se comporta como una clase extrema, sino como una clase intermedia y más difícil de separar mediante una frontera lineal.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{assets/figuras/modeling/confusion_svm.pdf}
    \caption{Matriz de confusión normalizada del SVM lineal sobre el conjunto de test}
    \label{fig:svm_confusion}
\end{figure}

El análisis de coeficientes del SVM lineal, mostrado en la figura \ref{fig:svm_coeficientes}, ofrece una aproximación a las variables con mayor peso en la frontera de decisión del modelo. Entre las variables más relevantes aparecen \texttt{possession\_home}, \texttt{passes\_home}, \texttt{score\_diff}, \texttt{passes\_away}, \texttt{carries\_home}, \texttt{goals\_away} y \texttt{goals\_home}. Estas variables están relacionadas principalmente con el control del balón, la circulación de juego y el marcador acumulado.

A diferencia de las importancias de variables de los modelos basados en árboles, estos coeficientes no deben interpretarse como una medida directa de importancia global. En este caso, reflejan el peso de cada variable en una combinación lineal utilizada para separar las clases. Por ello, deben interpretarse con prudencia: permiten observar qué variables influyen más en la frontera de decisión, pero no capturan interacciones complejas entre marcador, tiempo restante, xG y métricas acumuladas.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{assets/figuras/modeling/feature_importance_svm.pdf}
    \caption{Coeficientes medios absolutos de las 20 variables más relevantes en el SVM lineal}
    \label{fig:svm_coeficientes}
\end{figure}

Para el SVM lineal, la Permutation Importance muestra un patrón distinto al de los modelos basados en árboles. Como se observa en la figura \ref{fig:perm_svm}, la variable con mayor impacto es \texttt{possession\_home}, seguida de \texttt{attacks\_third\_away}, \texttt{score\_diff}, \texttt{goals\_minus\_xg\_home} y \texttt{goals\_away}. Este resultado es coherente con la naturaleza lineal del modelo, que tiende a apoyarse en combinaciones directas de variables de dominio, marcador y rendimiento acumulado. No obstante, como se ha observado en la matriz de confusión, estas variables no son suficientes para separar adecuadamente la clase empate.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{assets/figuras/modeling/permutation_importance_svm.pdf}
    \caption{Permutation Importance del SVM lineal sobre una muestra del conjunto de test}
    \label{fig:perm_svm}
\end{figure}

En conjunto, el SVM lineal cumple una función útil como modelo comparativo, ya que permite evaluar el rendimiento de una aproximación lineal frente a un problema con relaciones claramente complejas. Sus resultados muestran que parte de la información contenida en las variables temporales puede aprovecharse mediante una frontera simple, pero también evidencian que la predicción de empates y la separación equilibrada entre clases requieren modelos con mayor capacidad para capturar interacciones no lineales.

\paragraph{Evaluación de ejemplos individuales:}

El SVM lineal se evaluó sobre los mismos tres partidos y en los mismos minutos 30, 60 y 90. Esta comparación permite observar cómo se comporta el modelo lineal frente a los modelos basados en árboles en escenarios idénticos.

\begin{table}[H]
    \centering
    \resizebox{\textwidth}{!}{
        \begin{tabular}{lcccccc}
            \hline
            \textbf{match\_id} & \textbf{Minuto} & \textbf{P(local)} & \textbf{P(empate)} & \textbf{P(visitante)} & \textbf{Predicción} & \textbf{Acierto} \\
            \hline
            3775617            & 30              & 61,74 \%          & 17,05 \%           & 21,21 \%              & Local               & Sí               \\
            3775617            & 60              & 57,45 \%          & 25,90 \%           & 16,65 \%              & Local               & Sí               \\
            3775617            & 90              & 49,31 \%          & 33,95 \%           & 16,74 \%              & Local               & Sí               \\
            3893811            & 30              & 84,03 \%          & 14,23 \%           & 1,74 \%               & Local               & Sí               \\
            3893811            & 60              & 81,07 \%          & 16,84 \%           & 2,09 \%               & Local               & Sí               \\
            3893811            & 90              & 75,65 \%          & 22,02 \%           & 2,33 \%               & Local               & Sí               \\
            3920392            & 30              & 51,96 \%          & 30,45 \%           & 17,59 \%              & Local               & Sí               \\
            3920392            & 60              & 40,79 \%          & 36,92 \%           & 22,29 \%              & Local               & Sí               \\
            3920392            & 90              & 69,12 \%          & 28,20 \%           & 2,69 \%               & Local               & Sí               \\
            \hline
        \end{tabular}
    }
    \caption{Evolución de las probabilidades predichas por el SVM lineal en tres partidos del conjunto de test}
    \label{tab:svm_ejemplos_evolucion}
\end{table}

En estos tres partidos, el SVM lineal acierta las 9 predicciones analizadas. No obstante, este resultado debe interpretarse con prudencia, ya que los tres partidos seleccionados aleatoriamente terminan con victoria local y el SVM tiende a favorecer esta clase frente al empate. En el partido 3920392, por ejemplo, el modelo predice victoria local incluso en los minutos 30 y 60, cuando el marcador todavía está empatado. Esto le permite acertar el resultado final en este caso concreto, pero también es coherente con la dificultad observada del SVM para identificar empates en la matriz de confusión global. Por tanto, estos ejemplos muestran que el SVM puede funcionar bien en partidos que finalmente terminan con victoria local, pero no contradicen su limitación principal: la baja capacidad para separar correctamente la clase empate en situaciones más equilibradas.

\section{Evaluación}

\subsection{Evaluación del modelo final}

\subsubsection*{Comparación frente al baseline}

Como primer paso de la evaluación final, se compara el rendimiento de los modelos con una línea base sencilla. Esta línea base consiste en predecir siempre la clase mayoritaria del conjunto de test. En este caso, la clase más frecuente es \texttt{home\_win}, con 26.730 observaciones sobre un total de 62.280 filas, lo que equivale a un accuracy aproximado del 42,9 \%.

Esta comparación permite comprobar si los modelos entrenados aportan valor real respecto a una estrategia trivial. Si un modelo no superara claramente esta línea base, significaría que no estaría aprovechando de forma efectiva las variables construidas a partir de los eventos del partido.

\begin{table}[H]
    \centering
    \begin{tabular}{lcc}
        \hline
        \textbf{Modelo o estrategia} & \textbf{Accuracy} & \textbf{Mejora frente al baseline} \\
        \hline
        Baseline clase mayoritaria   & 42,9 \%           & --                                 \\
        Random Forest                & 65,0 \%           & +22,1 p.p.                         \\
        XGBoost                      & 65,4 \%           & +22,5 p.p.                         \\
        SVM lineal                   & 63,9 \%           & +21,0 p.p.                         \\
        \hline
    \end{tabular}
    \caption{Comparación de los modelos frente a la línea base de clase mayoritaria}
    \label{tab:comparacion_baseline}
\end{table}

Los tres modelos superan claramente la línea base. Esto indica que las variables temporales generadas durante la fase de preparación de datos contienen información útil para anticipar el resultado final del partido. XGBoost obtiene la mayor accuracy global, con un 65,4 \%, seguido muy de cerca por Random Forest, con un 65,0 \%. El SVM lineal también supera ampliamente el baseline, aunque su rendimiento debe interpretarse con más prudencia debido a su peor comportamiento en la clase empate, como se analiza en los apartados siguientes.

\subsubsection*{Selección del modelo final}

A partir de los resultados obtenidos, se selecciona XGBoost como modelo principal para el prototipo de predicción. Esta elección se basa en que es el modelo con mayor accuracy global y menor pérdida logarítmica sobre el conjunto de test. Dado que la aplicación propuesta no solo muestra una clase predicha, sino también la probabilidad asociada a cada posible resultado, la calidad de las probabilidades resulta especialmente relevante.

\begin{table}[H]
    \centering
    \begin{tabular}{lccc}
        \hline
        \textbf{Modelo} & \textbf{Accuracy} & \textbf{Macro-F1} & \textbf{Log-loss} \\
        \hline
        Random Forest   & 65,0 \%           & 63,3 \%           & 0,772             \\
        XGBoost         & 65,4 \%           & 62,7 \%           & 0,753             \\
        SVM lineal      & 63,9 \%           & 50,0 \%           & 0,798             \\
        \hline
    \end{tabular}
    \caption{Comparación final de los modelos evaluados sobre el conjunto de test}
    \label{tab:seleccion_modelo_final}
\end{table}

La figura \ref{fig:comparacion_modelos} muestra la evolución del accuracy y del F1-score macro por minuto para los tres modelos. Esta comparación permite observar que las diferencias entre Random Forest y XGBoost son reducidas, especialmente en fases avanzadas del partido. Ambos modelos mejoran de forma clara a medida que avanza el encuentro, mientras que el SVM lineal presenta un crecimiento más limitado, sobre todo en el F1-score macro.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{assets/figuras/modeling/comparison_rf_xgb_svm.pdf}
    \caption{Comparación de accuracy y Macro-F1 por minuto entre los modelos evaluados}
    \label{fig:comparacion_modelos}
\end{figure}

La diferencia entre XGBoost y Random Forest no es muy amplia. Random Forest alcanza el mejor F1-score macro, lo que indica un comportamiento algo más equilibrado entre clases. Sin embargo, XGBoost obtiene el mejor accuracy global y la menor pérdida logarítmica. Esto último es especialmente importante para el prototipo, ya que el sistema no solo debe mostrar una predicción final, sino también probabilidades asociadas a victoria local, empate y victoria visitante.

Por este motivo, XGBoost se considera el modelo principal del prototipo. Su rendimiento global es ligeramente superior en accuracy y ofrece las probabilidades más consistentes según la pérdida logarítmica. Aun así, Random Forest se mantiene como una alternativa muy competitiva, especialmente cuando se prioriza el equilibrio entre clases. En cambio, el SVM lineal queda por detrás como modelo final debido a su bajo F1-score macro y a la dificultad observada para identificar correctamente los empates.

Por tanto, el prototipo utilizará XGBoost como modelo principal, mientras que Random Forest y SVM lineal se conservarán como modelos de referencia para comparar el comportamiento de distintas estrategias de aprendizaje.

\subsubsection*{Análisis de errores y clases}

El análisis por clases permite observar que el rendimiento de los modelos no es homogéneo para los tres posibles resultados. Las victorias locales y visitantes se identifican con mayor facilidad que los empates, lo cual es coherente con la propia naturaleza del problema. En fútbol, el empate suele representar situaciones de mayor equilibrio entre equipos, y un único gol puede transformar rápidamente esta clase en victoria local o visitante.

\begin{table}[H]
    \centering
    \begin{tabular}{lccc}
        \hline
        \textbf{Modelo} & \textbf{Recall home\_win} & \textbf{Recall draw} & \textbf{Recall away\_win} \\
        \hline
        Random Forest   & 68 \%                     & 52 \%                & 70 \%                     \\
        XGBoost         & 72 \%                     & 44 \%                & 72 \%                     \\
        SVM lineal      & 87 \%                     & 3 \%                 & 78 \%                     \\
        \hline
    \end{tabular}
    \caption{Recall por clase de los modelos evaluados}
    \label{tab:recall_por_clase}
\end{table}

Random Forest consigue el mejor comportamiento en la clase empate, con un recall del 52 \%. Este resultado explica por qué obtiene el mejor F1-score macro, aunque no sea el modelo con mayor accuracy global. XGBoost, por su parte, presenta un rendimiento más equilibrado entre victorias locales y visitantes, con un recall del 72 \% en ambas clases, pero detecta peor los empates que Random Forest.

El caso del SVM lineal es distinto. Aunque alcanza un accuracy global aceptable, su recall para la clase empate es solo del 3 \%. Esto indica que el modelo tiende a clasificar gran parte de los empates como victorias locales o visitantes. Por tanto, su resultado global puede parecer razonable, pero el análisis por clases muestra una limitación importante. Este comportamiento justifica que el SVM se mantenga como modelo comparativo, pero no como modelo principal del sistema.

En conjunto, los errores más relevantes se concentran en los partidos igualados. Cuando el marcador y las métricas acumuladas muestran una ventaja clara para uno de los equipos, los modelos suelen generar predicciones coherentes. En cambio, cuando el partido está equilibrado o el marcador no refleja todavía una diferencia clara, aumenta la probabilidad de confusión entre empate y victoria de alguno de los equipos.

\subsubsection*{Evaluación probabilística}

Además de la clase predicha, en este trabajo es importante evaluar la calidad de las probabilidades generadas por los modelos. La herramienta propuesta no solo debe indicar si el resultado más probable es victoria local, empate o victoria visitante, sino también mostrar cómo se distribuye la probabilidad entre las tres opciones. Por este motivo, se utiliza la pérdida logarítmica como métrica probabilística.

\begin{table}[H]
    \centering
    \begin{tabular}{lc}
        \hline
        \textbf{Modelo} & \textbf{Log-loss} \\
        \hline
        Random Forest   & 0,772             \\
        XGBoost         & 0,753             \\
        SVM lineal      & 0,798             \\
        \hline
    \end{tabular}
    \caption{Pérdida logarítmica de los modelos sobre el conjunto de test}
    \label{tab:logloss_modelos}
\end{table}

XGBoost obtiene la menor pérdida logarítmica, con un valor de 0,753. Esto indica que sus probabilidades son, en conjunto, las más adecuadas entre los modelos evaluados. Aunque la diferencia con Random Forest no es muy amplia, este resultado refuerza la elección de XGBoost como modelo principal para el prototipo, ya que la aplicación final mostrará probabilidades y no únicamente una clase predicha.

Random Forest obtiene una pérdida logarítmica de 0,772. Su rendimiento es muy cercano al de XGBoost, pero ligeramente peor desde el punto de vista probabilístico. Aun así, su mejor F1-score macro muestra que sigue siendo un modelo muy competitivo, especialmente cuando se valora el equilibrio entre clases.

El SVM lineal obtiene la peor pérdida logarítmica, con un valor de 0,798. Este resultado es coherente con las limitaciones observadas en la matriz de confusión, especialmente en la clase empate. Además, en este modelo fue necesario calibrar las probabilidades, ya que \texttt{LinearSVC} no las proporciona directamente. La calibración permite comparar el SVM con los otros modelos mediante log-loss, pero no elimina completamente sus dificultades para separar las tres clases.

En resumen, la evaluación probabilística confirma que XGBoost es el modelo más adecuado cuando el objetivo es mostrar probabilidades al usuario. No obstante, las diferencias con Random Forest son reducidas, por lo que ambos modelos pueden considerarse alternativas sólidas dentro del sistema.

\subsubsection*{Limitaciones observadas durante la evaluación}

La evaluación realizada permite identificar varias limitaciones que deben tenerse en cuenta al interpretar los resultados. En primer lugar, el rendimiento global de los modelos no alcanza el 70 \% de accuracy considerando todos los minutos del partido. XGBoost, que obtiene el mejor accuracy global, alcanza un 65,4 \%. Sin embargo, el análisis por minuto muestra que este umbral sí se supera en fases avanzadas del encuentro, cuando el modelo dispone de más información acumulada.

En segundo lugar, la predicción de empates continúa siendo el principal reto del sistema. Aunque Random Forest mejora la detección de esta clase respecto al resto de modelos, los empates siguen siendo más difíciles de identificar que las victorias locales o visitantes. Esto se debe a que los partidos igualados pueden cambiar de clase por un único gol, especialmente en los minutos finales.

Otra limitación es la dependencia del marcador actual. Las variables más importantes en los modelos basados en árboles muestran que \texttt{score\_diff} tiene un peso muy elevado. Esto es coherente con el problema, ya que el marcador es una señal muy informativa, pero también implica que las predicciones son menos seguras cuando el marcador todavía no refleja claramente el desarrollo del partido.

También debe considerarse que la evaluación se realiza sobre datos históricos. El sistema simula una predicción dinámica utilizando únicamente la información disponible hasta cada minuto, pero no trabaja con datos en directo. Por tanto, los resultados muestran la viabilidad del enfoque en un entorno controlado, no el rendimiento de un sistema real conectado a una fuente de eventos en vivo.

Por último, los ejemplos individuales incluidos en el análisis sirven para interpretar casos concretos, pero no sustituyen la evaluación global. En esos ejemplos se observa cómo cambian las probabilidades en partidos concretos, pero las conclusiones principales deben basarse en las métricas calculadas sobre todo el conjunto de test.

En conjunto, la evaluación muestra que los modelos desarrollados superan claramente la línea base y permiten realizar predicciones útiles a medida que avanza el partido. A pesar de eso, los resultados también muestran que el problema sigue siendo complejo, especialmente en minutos tempranos y en partidos igualados. Estas limitaciones deben tenerse en cuenta en el uso del prototipo y en posibles ampliaciones futuras del sistema.

\section{Despliegue}

\subsection{Tecnología utilizada}

El prototipo de despliegue se ha implementado como una aplicación web interactiva utilizando \textit{Streamlit}, una biblioteca de Python orientada a la creación rápida de interfaces de datos. La elección de Streamlit frente a otros frameworks web como Flask o Django se debe a que el prototipo no requiere una arquitectura cliente-servidor compleja ni lógica de negocio adicional, sino mostrar de forma visual los resultados de un modelo de aprendizaje automático ya entrenado. Streamlit permite construir esta capa de presentación directamente en Python, sin necesidad de desarrollar una interfaz por separado en HTML o JavaScript, lo que reduce el tiempo de implementación y mantiene toda la lógica del sistema en un único lenguaje.

La aplicación se ha publicado en \textit{Streamlit Community Cloud}, la plataforma gratuita de despliegue ofrecida por los propios creadores de Streamlit. Esta plataforma permite publicar aplicaciones directamente desde un repositorio de GitHub sin necesidad de configurar servidores ni infraestructura adicional. Se descartó GitHub Pages como alternativa porque esta plataforma solo sirve contenido estático y no puede ejecutar código Python en el servidor, lo que la hace incompatible con cualquier aplicación que requiera inferencia en tiempo real o carga de modelos.

\subsection{Preparación de datos}

Los datasets y modelos generados durante el desarrollo tienen un tamaño demasiado elevado para incluirse directamente en el repositorio de GitHub, por lo que no pueden desplegarse tal cual en Streamlit Community Cloud. Para resolverlo, se ha desarrollado el script \texttt{prepare\_app\_data.py}, que se ejecuta una única vez en local antes de cada despliegue y genera los ficheros estrictamente necesarios para que la aplicación funcione.

El script filtra el dataset de modelado para conservar únicamente los partidos del conjunto de test, extrae los metadatos de cada partido y reduce el dataset de eventos a las columnas imprescindibles para la visualización. Los ficheros de datos resultantes se guardan en formato \textit{parquet} con compresión, y los modelos se copian o recomprimen en formato \textit{pickle}. Todo ello se almacena en la carpeta \texttt{app/data/}, de modo que todos los ficheros necesarios quedan dentro del límite de Git y pueden subirse al repositorio junto con el resto del código.

\subsection{Arquitectura de la aplicación}

El código de la aplicación se organiza en un número reducido de ficheros con responsabilidades bien delimitadas. El punto de entrada es \texttt{app.py}, que define la navegación entre las dos páginas mediante la API \texttt{st.navigation} de Streamlit, lo que permite asignar nombres y iconos a cada página con independencia del nombre del fichero. El módulo \texttt{data\_loader.py} centraliza todas las funciones de carga de datos y modelos con caché de sesión, de modo que los ficheros pesados se cargan una única vez con independencia de cuántas interacciones realice el usuario. Las dos páginas de la interfaz se encuentran en la carpeta \texttt{pages/} y son completamente independientes entre sí. Por último, la carpeta \texttt{data/} contiene los ficheros generados por \texttt{prepare\_app\_data.py} que se distribuyen junto con el código en el repositorio.

\subsection{Interfaz de la aplicación}

La interfaz se organiza en dos páginas accesibles desde el menú de navegación lateral. La primera permite explorar partidos reales del conjunto de test y consultar las predicciones del modelo a lo largo del encuentro. La segunda ofrece un simulador en el que el usuario puede construir escenarios hipotéticos de forma libre, sin necesidad de seleccionar un partido existente.

\subsubsection{Dashboard de partido}

La primera página de la aplicación permite explorar encuentros reales del conjunto de test y observar cómo evoluciona la predicción del modelo a lo largo del partido. En la parte superior, cuatro selectores encadenados permiten elegir el encuentro. El usuario escoge primero la competición, después la temporada disponible dentro de esa competición y por último el partido concreto, que aparece etiquetado con los dos equipos y el resultado final. Un cuarto selector permite cambiar el modelo con el que se realizan las predicciones entre los que se han incluido en el despliegue. Debajo de los selectores, un título destaca el partido elegido junto con una línea secundaria que recoge la competición, la temporada y el resultado final del encuentro.

El contenido se reparte en dos pestañas. La primera, denominada \textit{Explorador}, reconstruye el partido minuto a minuto y muestra la predicción en el instante seleccionado. La segunda, \textit{Evolución de probabilidades}, presenta la trayectoria completa de las probabilidades a lo largo de los noventa minutos.

En la pestaña \textit{Explorador}, un control deslizante permite situar cualquier minuto del encuentro entre el 1 y el 90, y todo lo que se muestra debajo se actualiza en función de ese valor. Justo debajo del deslizante aparece una cabecera con el nombre de ambos equipos y el marcador acumulado en el minuto escogido. A su derecha se muestra el bloque de probabilidades, formado por una barra que reparte el ancho disponible entre los tres posibles resultados y por tres cifras de gran tamaño que indican la probabilidad de victoria local, empate y victoria visitante.

La parte central de la pestaña es una línea de tiempo que representa los eventos del partido sobre dos carriles contiguos. El carril superior, en azul, corresponde al equipo local y el inferior, en rojo, al visitante, cada uno con el nombre del equipo dentro de su propia franja. Los eventos se dibujan con formas geométricas según su tipo, de manera que un círculo representa un gol, un triángulo un tiro y un rombo una parada. Cada evento incluye una etiqueta con el tipo, el minuto y, en el caso de los goles, el marcador resultante. Los eventos anteriores al minuto seleccionado se muestran con color pleno, mientras que los posteriores aparecen atenuados, lo que refuerza la sensación de avanzar por el partido. Una línea vertical naranja marca el minuto activo y atraviesa ambos carriles y una marca de descanso señala el final de la primera parte. Cuando varios eventos se concentran en pocos minutos, sus etiquetas se reparten en distintas alturas para evitar que se solapen.

Por debajo de la línea de tiempo, una tabla desplegable lista los eventos del partido hasta el minuto seleccionado, con un color de fondo distinto según el equipo al que pertenecen.

La pestaña \textit{Evolución de probabilidades} muestra, para el mismo partido, cómo varían las tres probabilidades a lo largo de todo el encuentro. Se representa una curva por cada posible resultado sobre los noventa minutos, con marcas verticales en los minutos en los que se produce un gol y una referencia horizontal en el valor de probabilidad que correspondería al azar. Esta vista permite analizar el partido en su conjunto y comprobar cómo los goles y la acumulación de ocasiones desplazan la predicción del modelo.


\subsubsection{Simulador}

La segunda página es un simulador que permite construir escenarios hipotéticos sin necesidad de seleccionar un partido real. El usuario define el estado del partido mediante un conjunto de controles agrupados en tres bloques:

\begin{itemize}
    \item \textbf{Marcador y tiempo}: goles de cada equipo, minuto actual, tipo de competición (masculina o femenina) y diferencial de jugadores por expulsiones.
    \item \textbf{Producción ofensiva}: tiros y \textit{expected goals} de cada equipo.
    \item \textbf{Control del juego}: posesión y precisión de pase de ambos equipos.
\end{itemize}

A partir de estos valores, la aplicación construye el vector de 48 variables que requiere el modelo. Las variables que el usuario no controla directamente se inicializan con la mediana del conjunto de test como estado neutro, y las variables derivadas como la diferencia de goles, la diferencia de xG o los goles sobre xG se calculan automáticamente a partir de los valores introducidos. El usuario puede seleccionar el modelo con el que se realiza la predicción y el resultado se muestra de forma inmediata mediante una barra y tres cifras con la probabilidad de victoria local, empate y victoria visitante.